<!-- Sidebar -->
<aside
    class="hidden md:flex md:flex-col bg-white dark:bg-neutral-900 border-r border-gray-200 dark:border-neutral-800">

    <!-- header -->
    <div class="px-4 pt-3 pb-2">
        <div class="flex items-center justify-between">
            <h2 class="text-[15px] font-semibold text-gray-900 dark:text-gray-100">Chats</h2>
            <button class="p-1.5 rounded hover:bg-gray-100 dark:hover:bg-neutral-800">
                <svg class="size-4 text-gray-500 dark:text-gray-400" viewBox="0 0 20 20" fill="currentColor">
                    <path
                        d="M6 10a2 2 0 11-4 0 2 2 0 014 0zm6 0a2 2 0 11-4 0 2 2 0 014 0zm6 0a2 2 0 11-4 0 2 2 0 014 0z"/>
                </svg>
            </button>
        </div>

        <!-- search -->
        <label class="relative mt-2 block">
            <input type="search"
                   class="w-full rounded-lg border border-gray-300 dark:border-neutral-700 bg-white dark:bg-neutral-900 pl-9 pr-3 py-2 text-sm placeholder-gray-400 dark:placeholder-neutral-500 outline-none focus:ring-2 focus:ring-blue-500/30"
                   placeholder="Search for messages or contacts"/>
            <svg class="size-5 absolute left-2.5 top-2.5 text-gray-400 dark:text-neutral-500"
                 viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
                      d="m21 21-4.3-4.3m0-6.2a7.5 7.5 0 1 1-15 0 7.5 7.5 0 0 1 15 0Z"/>
            </svg>
        </label>
    </div>

    <!-- list -->
    <nav class="flex-1 overflow-y-auto thin-scroll px-2 pb-3 space-y-4">

        <!-- PRIVATE CONVERSATIONS -->
        <div>
            <div class="px-2 pt-1 pb-2 text-xs uppercase tracking-wide text-gray-500 dark:text-neutral-400">
                Private conversations
            </div>
            @foreach (var privateConversation in Conversations.Where(c => c.Type == ConversationType.Private))
            {
                <button onclick="@(async () => await OnConversationSelected.InvokeAsync(privateConversation))"
                    class="w-full flex items-start gap-3 px-2 py-2 rounded-lg hover:bg-gray-50 dark:hover:bg-neutral-800">
                    <span class="relative">
                        <img class="size-10 rounded-full"
                             src="@(privateConversation.Image is null ? "/images/user-avatar.png" : privateConversation.Image.Url)"
                             alt="">
                        <span
                            class="absolute -bottom-0.5 -right-0.5 size-2.5 rounded-full bg-rose-500 ring-2 ring-white dark:ring-neutral-900"></span>
                    </span>
                    <span class="min-w-0 flex-1 text-left">
                        <span class="flex items-center justify-between">
                            <span class="text-[15px] font-medium truncate">@privateConversation.Title</span>
                            @if (privateConversation.LastMessageAtUtc.HasValue)
                            {
                                <span class="text-[11px] text-gray-500">@privateConversation.LastMessageAtUtc.Value.ToString("HH:mm")</span>   
                            }
                        </span>
                        <span
                            class="mt-0.5 text-xs text-gray-500 dark:text-neutral-400 truncate">@(privateConversation.Messages.FirstOrDefault() is not null ? privateConversation.Messages.FirstOrDefault()?.Content : "")</span>
                    </span>
                </button>
            }
        </div>

        <!-- MATCH CONVERSATIONS -->
        <div>
            <div class="px-2 pt-1 pb-2 text-xs uppercase tracking-wide text-gray-500 dark:text-neutral-400">
                Match conversations
            </div>

            @foreach (var privateConversation in Conversations.Where(c => c.Type == ConversationType.Match))
            {
                <button onclick="@(async () => await OnConversationSelected.InvokeAsync(privateConversation))"
                    class="w-full flex items-start gap-3 px-2 py-2 rounded-lg hover:bg-gray-50 dark:hover:bg-neutral-800">
                    <span class="relative">
                        <img class="size-10 rounded-full"
                             src="@(privateConversation.Image is null ? "/images/user-avatar.png" : privateConversation.Image.Url)"
                             alt="">
                        <span
                            class="absolute -bottom-0.5 -right-0.5 size-2.5 rounded-full bg-rose-500 ring-2 ring-white dark:ring-neutral-900"></span>
                    </span>
                    <span class="min-w-0 flex-1 text-left">
                        <span class="flex items-center justify-between">
                            <span class="text-[15px] font-medium truncate">@privateConversation.Title</span>
                            @if (privateConversation.LastMessageAtUtc.HasValue)
                            {
                                <span class="text-[11px] text-gray-500">@privateConversation.LastMessageAtUtc.Value.ToString("HH:mm")</span>   
                            }
                        </span>
                        <span
                            class="mt-0.5 text-xs text-gray-500 dark:text-neutral-400 truncate">@(privateConversation.Messages.FirstOrDefault() is not null ? privateConversation.Messages.FirstOrDefault()?.Content : "")</span>
                    </span>
                </button>
            }

        </div>

        <!-- TEAM CONVERSATIONS -->
        <div>
            <div class="px-2 pt-1 pb-2 text-xs uppercase tracking-wide text-gray-500 dark:text-neutral-400">Team
                conversations
            </div>

            @foreach (var privateConversation in Conversations.Where(c => c.Type == ConversationType.Team))
            {
                <button onclick="@(async () => await OnConversationSelected.InvokeAsync(privateConversation))"
                    class="w-full flex items-start gap-3 px-2 py-2 rounded-lg hover:bg-gray-50 dark:hover:bg-neutral-800">
                    <span class="relative">
                        <img class="size-10 rounded-full"
                             src="@(privateConversation.Image is null ? "/images/user-avatar.png" : privateConversation.Image.Url)"
                             alt="">
                        <span
                            class="absolute -bottom-0.5 -right-0.5 size-2.5 rounded-full bg-rose-500 ring-2 ring-white dark:ring-neutral-900"></span>
                    </span>
                    <span class="min-w-0 flex-1 text-left">
                        <span class="flex items-center justify-between">
                            <span class="text-[15px] font-medium truncate">@privateConversation.Title</span>
                            @if (privateConversation.LastMessageAtUtc.HasValue)
                            {
                                <span class="text-[11px] text-gray-500">@privateConversation.LastMessageAtUtc.Value.ToString("HH:mm")</span>   
                            }
                        </span>
                        <span
                            class="mt-0.5 text-xs text-gray-500 dark:text-neutral-400 truncate">@(privateConversation.Messages.FirstOrDefault() is not null ? privateConversation.Messages.FirstOrDefault()?.Content : "")</span>
                    </span>
                </button>
            }


        </div>

    </nav>
</aside>

@code
{
    [Parameter] public List<Conversation> Conversations { get; set; } = new();
    [Parameter] public EventCallback<Conversation> OnConversationSelected { get; set; }
}
